# prompt-state-rules

## 対象

- プロンプト設計機能

## このファイルに記載する内容

- 状態一覧
- バージョン管理ルール
- 状態遷移表
- 操作可否マトリクス
- 例外処理一覧
- Servlet / Service / Repository の責務分担

## 初期メモ

- 状態は `下書き` / `設定済み` / `再評価済み` を基本とする。
- バージョンは `ver.3` / `ver.3.1` / `ver.3.1.4` の階層表記を採用する。
- 履歴の編集は過去データの直接上書きではなく、読み戻して新しい下書きとして扱う。
